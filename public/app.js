import {parseInvite,inviteFromLocation} from './invite.js';
import {LiveSession} from './live.js';
import {affiliates,initial,uid,move,choose,validate,addLastYearRoster,reactionOptions,toggleReaction,reactionsFor} from './model.js';
const $=s=>document.querySelector(s), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const key='cubs-coaching-v1';let state=initial(),active,profile='matt',dragged=null;
try{const raw=localStorage.getItem(key);if(raw)state=validate(JSON.parse(raw));profile=localStorage.getItem(key+'-profile')||state.profiles[0].id;}catch{setTimeout(()=>toast('Saved board could not be loaded. Import a backup to recover.'),100);}
const rosterAdded = addLastYearRoster(state);
if(!state.profiles.some(p=>p.id===profile))profile=state.profiles[0].id;active=rosterAdded ? state.scenarios[state.scenarios.length-1].id : state.scenarios[0].id;
const hostSessionKey='cubs-host-session';
const unseenScenarios=new Set();
let live, stateGeneration=0, personalState, unsentDraft, joiningName, reactionTarget;
const board=()=>state.scenarios.find(s=>s.id===active), actor=()=>state.profiles.find(p=>p.id===profile).name;
function toast(t){$('#toast').textContent=t;$('#toast').style.display='block';clearTimeout(toast.timer);toast.timer=setTimeout(()=>$('#toast').style.display='none',4200);}
function storageKey(){return live?.mode==='guest'?key+'-session-'+live.host:key;}
function persist(){try{localStorage.setItem(storageKey(),JSON.stringify(state));localStorage.setItem(key+'-profile',profile);$('#saved').textContent=live?.mode==='guest'?'● Session copy on this browser':'● Saved on this browser';}catch{$('#saved').textContent='Not saved — export a backup';toast('Browser storage is full or unavailable. Export a backup now.');}render();}
function save(){stateGeneration++;persist();if(live?.mode!=='off'&&live){try{live.publish(state);}catch(e){toast(e.message);}}}
function log(text){board().events.unshift({actor:actor(),text,date:new Date().toISOString()});}
function card(c){const p=board().placements[c.id];return `<button class="card ${p?.final?'final':''}" draggable="true" data-card="${esc(c.id)}"><strong>${esc(c.name)}</strong><small>${esc(c.title||'Position to discuss')}${p?.final?' · Final choice':''}</small></button>`;}
function render(){updateScenarioSharing();const b=board();renderReactions(b);$('#profile').innerHTML=state.profiles.map(p=>`<option value="${esc(p.id)}" ${profile===p.id?'selected':''}>${esc(p.name)}</option>`).join('');$('#scenarios').innerHTML=state.scenarios.map(s=>`<button data-scenario="${esc(s.id)}" aria-label="${esc(s.name)}" title="${esc(s.createdBy?'Created by '+s.createdBy:s.name)}" class="${s.id===active?'active':''}">${esc(s.name)}${unseenScenarios.has(s.id)?'<span class="scenario-new" aria-hidden="true">NEW</span>':''}${reactionSummary(s)}</button>`).join('');$('#scenarioTitle').textContent=b.name;const finals=Object.values(b.placements).filter(p=>p.final).length;$('#summary').textContent=`${finals} / ${state.roles.length*affiliates.filter(a=>a[0]!=='release').length} positions decided`;const pool=state.candidates.filter(c=>!b.placements[c.id]||b.placements[c.id].aff==='pool');$('#poolCount').textContent=pool.length;const q=$('#search').value.toLowerCase();$('#pool').innerHTML=pool.filter(c=>(c.name+' '+c.title).toLowerCase().includes(q)).map(card).join('')||`<div class="empty-pool"><span>＋</span>${q?'No matching candidates':'A fresh board.<br>Add your first candidate.'}</div>`;
$('#board').style.setProperty('--column-count',affiliates.length);
$('#board').innerHTML=affiliates.map(([id,level,name,club,color,logo=id])=>`<article class="affiliate ${id==='release'?'release-column':''}" style="--team:${color}"><div class="affiliate-head"><img src="assets/${logo}.png" alt="${name} ${club}"><div><div class="level">${level}</div><h2>${name}</h2><small>${club}</small></div></div>${(id==='release'?['']:state.roles).map(role=>{const cs=state.candidates.filter(c=>b.placements[c.id]?.aff===id&&b.placements[c.id]?.role===role);return `<div class="slot drop" data-aff="${id}" data-role="${esc(role)}"><div class="slot-title"><span>${id==='release'?'RELEASE OPTIONS':esc(role.toUpperCase())}</span><span>${cs.length||'—'}</span></div>${cs.map(card).join('')}${cs.length?'':`<button class="slot-empty" data-add-aff="${id}" data-add-role="${esc(role)}">＋ Add or drop candidate</button>`}${positionReactionControl(b,id,role)}</div>`;}).join('')}${id==='release'?'<p class="release-help">A scenario option. Move names back to any position at any time.</p>':''}</article>`).join('');
if(reactionTarget&&$('#modal').open){if(reactionTarget.aff!=='release'&&!state.roles.includes(reactionTarget.role)){close();toast('That position was removed.');}else $('#fields').innerHTML=reactionPanel(b,reactionTarget);}

$('#activity').innerHTML=b.events.length?b.events.map(e=>`<div class="event"><b>${esc(e.actor)}</b> ${esc(e.text)}<time>${esc(new Date(e.date).toLocaleString())}</time></div>`).join(''):'<p class="muted">A fresh start. Your team’s moves and opinions will appear here.</p>';}
function modal(title,fields,buttons,submit){const generation=stateGeneration;$('#modalTitle').textContent=title;$('#fields').innerHTML=fields;$('#modalActions').innerHTML=buttons;$('#form').onsubmit=e=>{e.preventDefault();if(generation!==stateGeneration){toast('The board changed while this form was open. Copy any text you need, then close and reopen it.');return;}submit(new FormData(e.target));};$('#modal').showModal();}
const input=(label,name,value='')=>`<label for="f-${name}">${label}</label><input id="f-${name}" name="${name}" value="${esc(value)}" required maxlength="150">`;
const close=()=>{reactionTarget=undefined;$('#modal').close();};$('#modal').addEventListener('cancel',()=>reactionTarget=undefined);$('#close').onclick=close;
function add(aff='pool',role=''){modal('Add candidate',input('Candidate name','name')+input('Position / title','title',role),'<button class="primary">Add candidate</button>',f=>{const name=f.get('name').trim(),title=f.get('title').trim();if(!name||!title)return;const c={id:uid(),name,title};state.candidates.push(c);if(aff!=='pool')move(state,board(),c.id,aff,role,actor());log(`added ${name}${aff==='pool'?' to the candidate pool':' to '+aff+' / '+role}`);save();close();});}
function edit(id){const editGeneration=stateGeneration;const c=state.candidates.find(c=>c.id===id),p=board().placements[id]||{aff:'pool',role:''};modal(c.name,input('Candidate name','name',c.name)+input('Position / title','title',c.title)+`<label for="destination">Board position</label><select name="destination" id="destination"><option value="pool">Candidate pool</option>${affiliates.map(([a,l,n])=>a==='release'?`<option value="${esc(JSON.stringify(['release','']))}" ${p.aff==='release'?'selected':''}>Release — Under consideration</option>`:state.roles.map(r=>`<option value="${esc(JSON.stringify([a,r]))}" ${p.aff===a&&p.role===r?'selected':''}>${l} ${n} — ${esc(r)}</option>`).join('')).join('')}</select><label for="opinion">Your opinion · ${esc(actor())}</label><textarea name="opinion" id="opinion" placeholder="Why is this a good fit? What should we consider?" maxlength="3000"></textarea><div class="notes">${board().notes.filter(n=>n.candidate===id).map(n=>`<p><b>${esc(n.actor)}</b> · ${esc(n.text)}</p>`).join('')}</div>`,`<button class="primary">Save changes</button><button type="button" id="finalize" ${['pool','release'].includes(p.aff)?'disabled':''}>${p.final?'☆ Reopen choice':'★ Mark final'}</button><button type="button" id="remove" class="danger">Delete candidate</button>`,f=>{const name=f.get('name').trim(),title=f.get('title').trim();if(!name||!title)return;const dest=f.get('destination');if(c.name!==name||c.title!==title){c.name=name;c.title=title;log(`updated ${name}’s details`);}const [a,r]=dest==='pool'?['pool','']:JSON.parse(dest);if(a!==p.aff||r!==p.role)log('moved '+move(state,board(),id,a,r,actor()));const note=f.get('opinion').trim();if(note){board().notes.push({candidate:id,actor:actor(),text:note,date:new Date().toISOString()});log(`commented on ${c.name}: “${note}”`);}save();close();});$('#finalize').onclick=()=>{if(editGeneration!==stateGeneration){toast('Board updated. Close and reopen this card.');return;}choose(board(),id);log(`${board().placements[id].final?'selected':'reopened'} ${c.name} for ${p.aff} / ${p.role}`);save();close();};$('#remove').onclick=()=>{if(editGeneration!==stateGeneration){toast('Board updated. Close and reopen this card.');return;}if(!confirm(`Delete ${c.name} from every scenario?`))return;state.candidates=state.candidates.filter(x=>x.id!==id);for(const s of state.scenarios){delete s.placements[id];s.notes=s.notes.filter(n=>n.candidate!==id);}log(`deleted ${c.name} from all scenarios`);save();close();};}
$('#add').onclick=()=>add();$('#poolAdd').onclick=()=>add();$('#search').oninput=render;$('#profile').onchange=e=>{profile=e.target.value;persist();};$('#profiles').onclick=()=>modal('Leadership profile',input('Full name','name')+'<p class="muted">Select this profile to attribute future moves and opinions. Profiles label who is making a change; they are not secure sign-ins.</p>','<button class="primary">Create profile</button>',f=>{const name=f.get('name').trim();if(!name)return;const p={id:uid(),name};state.profiles.push(p);profile=p.id;save();close();});
function scenario(duplicate=false){modal(duplicate?'Duplicate scenario':'New scenario',input('Scenario name','name',duplicate?board().name+' — alternative':''),'<button class="primary">Create scenario</button>',f=>{const name=f.get('name').trim();if(!name)return;const b=duplicate?structuredClone(board()):{placements:{},notes:[],events:[]};b.id=uid();b.name=name;b.createdBy=actor();b.reactions=[];state.scenarios.push(b);active=b.id;log(duplicate?'created an alternative from an existing scenario':'created this scenario');save();close();});}
$('#newScenario').onclick=()=>scenario();$('#duplicate').onclick=()=>scenario(true);$('#rename').onclick=()=>modal('Rename scenario',input('Scenario name','name',board().name),'<button class="primary">Save name</button>',f=>{const n=f.get('name').trim();if(n){board().name=n;save();close();}});
$('#roles').onclick=()=>modal('Position titles',`<label for="rolesText">One title per line · applies to every affiliate</label><textarea id="rolesText" name="roles" required>${esc(state.roles.join('\n'))}</textarea><p class="muted">Removing a position returns its candidates to the pool in every scenario.</p>`,'<button class="primary">Save positions</button>',f=>{const roles=[...new Set(f.get('roles').split('\n').map(x=>x.trim()).filter(Boolean))];if(!roles.length||roles.some(r=>r.length>150)){toast('Enter at least one title, up to 150 characters each.');return;}for(const s of state.scenarios)for(const p of Object.values(s.placements))if(!['pool','release'].includes(p.aff)&&!roles.includes(p.role)){p.aff='pool';p.role='';p.final=false;}state.roles=roles;for(const s of state.scenarios)s.reactions=(s.reactions||[]).filter(r=>!r.target||r.target.aff==='release'||roles.includes(r.target.role));log('updated position titles across all scenarios');save();close();});
document.addEventListener('click',e=>{const reaction=e.target.closest('[data-reaction]');if(reaction){const target=reaction.dataset.target?JSON.parse(reaction.dataset.target):undefined;toggleReaction(state,board(),profile,reaction.dataset.reaction,target);save();return;}const position=e.target.closest('[data-position-reactions]');if(position){openPositionReactions(JSON.parse(position.dataset.positionReactions));return;}const c=e.target.closest('[data-card]');if(c)return edit(c.dataset.card);const s=e.target.closest('[data-scenario]');if(s){active=s.dataset.scenario;unseenScenarios.delete(active);render();}const a=e.target.closest('[data-add-aff]');if(a)add(a.dataset.addAff,a.dataset.addRole);});
document.addEventListener('dragstart',e=>{const c=e.target.closest('[data-card]');if(!c)return;dragged=c.dataset.card;e.dataTransfer.setData('text/plain',dragged);e.dataTransfer.effectAllowed='move';c.classList.add('dragging');});document.addEventListener('dragover',e=>{const z=e.target.closest('.drop');if(z&&dragged){e.preventDefault();z.classList.add('over');}});document.addEventListener('dragleave',e=>{const z=e.target.closest('.drop');if(z&&!z.contains(e.relatedTarget))z.classList.remove('over');});document.addEventListener('drop',e=>{const z=e.target.closest('.drop');if(!z||!dragged)return;e.preventDefault();const p=board().placements[dragged];if(p?.aff!==z.dataset.aff||p?.role!==z.dataset.role)log('moved '+move(state,board(),dragged,z.dataset.aff,z.dataset.role,actor()));dragged=null;save();});document.addEventListener('dragend',()=>{dragged=null;document.querySelectorAll('.over,.dragging').forEach(e=>e.classList.remove('over','dragging'));});
$('#export').onclick=()=>{const url=URL.createObjectURL(new Blob([JSON.stringify(state,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download='coaching-board-'+new Date().toISOString().slice(0,10)+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toast('Board exported with all scenarios, profiles, and opinions.');};$('#import').onclick=()=>$('#file').click();$('#file').onchange=async e=>{const file=e.target.files[0];if(!file)return;try{if(file.size>5e6)throw Error('File exceeds 5 MB');const incoming=validate(JSON.parse(await file.text()));if(!confirm('Replace this browser’s board with the imported board? Export your current board first if you need a backup.'))return;state=incoming;active=state.scenarios[0].id;profile=state.profiles[0].id;save();toast('Board imported.');}catch(err){toast('Import failed: '+err.message);}finally{e.target.value='';}};
function downloadBoard(data,name){const url=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download=name+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
live=new LiveSession({
  read:()=>state,
  receive:next=>{const added=next.scenarios.filter(s=>!state.scenarios.some(existing=>existing.id===s.id));if(!joiningName){for(const s of added)unseenScenarios.add(s.id);if(added.length)toast(added.length===1?`New shared scenario: ${added[0].name}. Click its NEW tab to open it.`:`${added.length} new shared scenarios are ready. Look for the NEW tabs.`);}state=next;stateGeneration++;if(!state.scenarios.some(s=>s.id===active))active=state.scenarios[0].id;if(!state.profiles.some(p=>p.id===profile))profile=state.profiles[0].id;persist();if(joiningName){const name=joiningName;joiningName=null;let p=state.profiles.find(p=>p.name===name);if(!p){p={id:uid(),name};state.profiles.push(p);}profile=p.id;save();}},
  status:message=>{$('#liveStatus').textContent=message;$('#liveStart').hidden=live.mode!=='off';$('#liveJoin').hidden=live.mode!=='off';$('#liveRetry').hidden=!live.failed;if(live.mode==='host'&&live.host){try{sessionStorage.setItem(hostSessionKey,live.host);}catch{}}$('#liveCopy').hidden=live.mode!=='host'||!live.ready;$('#liveLeave').hidden=live.mode==='off';updateScenarioSharing();},
  notice:toast,
  pending:value=>{document.body.classList.toggle('live-pending',value);},
  recovery:draft=>{unsentDraft=structuredClone(draft);$('#liveRecovery').hidden=false;}
});
$('#liveRecovery').onclick=()=>downloadBoard(unsentDraft,'coaching-unsent-draft');
$('#liveStart').onclick=()=>{
  modal('Brainstorm together', '<p>Share your current board in a live session. Your partner can move names, add candidates, and create scenarios with you.</p><p><b>Keep this tab open while you work together.</b> Anyone with the invite link can join and edit. Your partner’s own local board stays separate.</p>', '<button class="primary">Start live session</button>',()=>{close();live.start('host');});
};
$('#liveCopy').onclick=async()=>{try{await navigator.clipboard.writeText(live.link());toast('Invite link copied. Send it to your partner and keep this tab open.');}catch{modal('Session invite','<label for="inviteLink">Copy this link and send it to your partner</label><input id="inviteLink" readonly value="'+esc(live.link())+'">','<button class="primary">Done</button>',close);}};
$('#liveLeave').onclick=()=>{try{sessionStorage.removeItem(hostSessionKey);}catch{}const guest=live.mode==='guest';live.stop();joiningName=null;if(guest&&personalState){state=personalState;personalState=null;active=state.scenarios[0].id;if(!state.profiles.some(p=>p.id===profile))profile=state.profiles[0].id;stateGeneration++;persist();}const cleanUrl=new URL(location.href);cleanUrl.hash='';cleanUrl.searchParams.delete('join');history.replaceState(null,'',cleanUrl);lastInvite=null;toast('Session closed. You are working locally.');};
// While waiting for host confirmation, only session controls and exports remain available.
document.addEventListener('click',e=>{if(!live.blocked)return;if(e.target.closest('.live-bar, #export, #close'))return;e.preventDefault();e.stopImmediatePropagation();toast('Live updates are paused. Wait for connection, or leave the session to work locally.');},true);
document.addEventListener('dragstart',e=>{if(live.blocked){e.preventDefault();e.stopImmediatePropagation();}},true);
document.addEventListener('drop',e=>{if(live.blocked){e.preventDefault();e.stopImmediatePropagation();}},true);
document.addEventListener('submit',e=>{if(live.blocked){e.preventDefault();e.stopImmediatePropagation();}},true);
window.addEventListener('beforeunload',e=>{if(live.mode==='host'&&live.connections.size){e.preventDefault();e.returnValue='';}});
if (rosterAdded) save(); else render();
let lastInvite=null;
function promptJoin(host){
  if(live.mode!=='off') {toast('Leave your current session before joining a different one.');return;}
  if($('#modal').open)close();
  modal('Join shared board',input('Your name / nickname','name')+'<p>The host must have their live session open. Your personal board stays separate.</p>','<button class="primary">Join live session</button>',f=>{const name=f.get('name').trim();if(!name)return;personalState=structuredClone(state);joiningName=name;close();live.start('guest',host);});
}
function checkInvite(){
  const raw=inviteFromLocation(location.href);
  if(!raw||raw===lastInvite)return;
  const host=parseInvite(raw);lastInvite=raw;
  if(!host){toast('This invite is incomplete. Use Join session to paste the full invite or ask the host to copy it again.');return;}
  if(live.mode==='host'&&live.host===host){toast('This is your own session. Send the invite to your partner.');return;}
  promptJoin(host);
}
$('#liveJoin').onclick=()=>modal('Join a live session',input('Invite link or session code','invite')+'<p>Paste the full invite link your partner sent you.</p>','<button class="primary">Continue</button>',f=>{const host=parseInvite(f.get('invite'));if(!host){toast('Paste a complete invite link or a session code beginning with coaching-.');return;}close();promptJoin(host);});
$('#liveRetry').onclick=()=>{const role=live.mode,host=live.host;if(role==='off')return;live.start(role,host);};
window.addEventListener('hashchange',checkInvite);
window.addEventListener('popstate',checkInvite);
checkInvite();
if(!inviteFromLocation(location.href)){let resume;try{resume=sessionStorage.getItem(hostSessionKey);}catch{}if(parseInvite(resume))live.start('host',resume);}

function reactionSummary(scenario, target) {
  const summary = reactionOptions.map(([key,emoji]) => {
    const count = reactionsFor(scenario,target).filter(r => r.emoji === key).length;
    return count ? `${emoji} ${count}` : '';
  }).filter(Boolean).join(' ');
  return summary ? `<span class="reaction-summary" aria-hidden="true">${summary}</span>` : '';
}
function positionLabel(target){const name=affiliates.find(a=>a[0]===target.aff)?.[2]||target.aff;return target.aff==='release'?'Release options':name+' · '+target.role;}
function positionReactionControl(scenario,aff,role){const target={aff,role};return `<button type="button" class="position-react" data-position-reactions="${esc(JSON.stringify(target))}" aria-label="React to ${esc(positionLabel(target))}">☺ React${reactionSummary(scenario,target)}</button>`;}
function openPositionReactions(target){
  reactionTarget=target;
  modal('React to '+positionLabel(target),reactionPanel(board(),target),'<button type="button" id="reactionDone" class="primary">Done</button>',close);
  $('#reactionDone').onclick=close;
}
function reactionPanel(scenario,target){
  const groups = reactionOptions.map(([key,emoji,label]) => {
    const votes = reactionsFor(scenario,target).filter(r => r.emoji === key);
    const names = votes.map(r => state.profiles.find(p => p.id === r.profile)?.name || 'Unknown');
    return {key,emoji,label,votes,names,mine:votes.some(r => r.profile === profile)};
  });
  return `<div class="reaction-heading"><strong>${target?'Feedback on '+esc(positionLabel(target)):'How do you feel about this entire roster?'}</strong><span>Reacting as ${esc(actor())} · Click again to remove</span></div><div class="reaction-buttons">${groups.map(g => `<button type="button" data-reaction="${g.key}" ${target?`data-target="${esc(JSON.stringify(target))}"`:''} aria-label="${g.label}" aria-pressed="${g.mine}" title="${esc(g.names.length ? g.names.join(', ') : g.label)}"><span class="reaction-emoji" aria-hidden="true">${g.emoji}</span> ${g.label}<span class="reaction-count">${g.votes.length}</span></button>`).join('')}</div><div class="reaction-people" aria-live="polite">${groups.filter(g => g.votes.length).map(g => `<span>${g.emoji} ${esc(g.names.join(', '))}</span>`).join('') || '<span>No reactions yet. Give this option a little feedback.</span>'}</div>`;
}
function renderReactions(scenario){$('#reactions').innerHTML=reactionPanel(scenario);}

function updateScenarioSharing(){
  const el=$('#scenarioSharing');if(!el)return;
  if(live?.mode==='off'||!live)el.textContent='Create a live session to share all scenario tabs with your partners.';
  else if(!live.ready)el.textContent='Connecting — scenario sharing is paused until the session is live.';
  else el.textContent=`All ${state.scenarios.length} scenario tabs are shared with everyone in this session. Click any tab to explore it.`;
}
